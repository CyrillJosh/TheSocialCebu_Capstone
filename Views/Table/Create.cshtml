@model TheSocialCebu_Capstone.ViewModels.TableVM

@{
    ViewData["Title"] = "Create Table";
}

<div class="render-body h-100 w-100">
    <div class="text-white p-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="mb-0 text-dark fw-bold text-antonio">CREATE NEW TABLE</h1>
            <div>
                <button type="submit" form="tableForm" class="btn btn-success fw-bold me-2 px-4">Save</button>
                <a asp-action="Index" class="btn btn-secondary px-4">Cancel</a>
            </div>
        </div>

        <form asp-action="Create" enctype="multipart/form-data" id="tableForm">
            <div class="btn-group" role="group" aria-label="Status" hidden>
                <input type="radio" class="btn-check" name="StatusId" id="1" value="1" autocomplete="off" checked>
            </div>

            <input hidden asp-for="Id" value="@Guid.NewGuid().ToString()" id="TableID"/>
            <div class="mb-3">
                <label asp-for="TableNumber" class="text-dark"></label>
                <input asp-for="TableNumber" placeholder="Table Number" class="form-control bg-body-secondary text-dark border-secondary" />
                <span asp-validation-for="TableNumber" class="text-danger small"></span>
            </div>

            <div class="mb-3">
				<label asp-for="LocationId" class="text-dark">Location</label>
                <select asp-for="LocationId" asp-items="Model.LocationList" class="form-select bg-body-secondary text-dark border-secondary">
                    <option value="" class="bg-body-secondary">-- Select a Location --</option>
                </select>
                <span asp-validation-for="LocationId" class="text-danger small"></span>
            </div>


            <div class="mb-4">
                <label class="form-label text-dark">QR Code Preview</label><br />
                <img id="qrPreview" src="" alt="QR Code Preview" class="img-thumbnail rounded shadow border border-secondary" style="max-width: 400px; display: none;" />
            </div>

            <input type="hidden" name="QRCodeBase64" id="QRCodeBase64" />
        </form>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
        const tableid = $("#TableID").val()
        // console.log(tableid)
        fetch(`/Table/GenerateQRCode?value=${tableid}`)
            .then(response => response.json())
            .then(data => {
                const preview = document.getElementById('qrPreview');
                // console.log(data);

                preview.src = data.qrstring;
                preview.style.display = 'block';

                document.getElementById('QRCodeBase64').value = data.qrstring;
            })
            .catch(error => {
               AlertModal("Error",'Problem in generating QR code');
            });
        })
    </script>
}
