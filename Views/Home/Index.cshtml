@using System.Text.Json
@using TheSocialCebu_Capstone.Models;
@model List<Category>
@{
    ViewData["Title"] = "Home Page";
    var table = Context.Session.GetString("Table");
    var ordersJson = Context.Session.GetString("Orders");
    var orders = string.IsNullOrEmpty(ordersJson)
       ? new List<OrderItem>()
       : JsonSerializer.Deserialize<List<OrderItem>>(ordersJson);
}
<div class="render-body d-flex flex-column vh-100 overflow-y-scroll"
     style="background: radial-gradient(376.79% 162.09% at 141.41% 59.77%, #000000 21.61%, #585757 56.69%, #565555 94.59%, #000000 100%);">

    <!-- Top Nav -->
    <nav class="nav flex-row bg-transparent w-100 p-2 sticky-top" style="z-index: 1020;">
        <div class="w-100">
            <div class="px-3 row align-items-center">
                <div class="col-7">
                    <p class="text-light fw-bold display-6 m-0">THE SOCIAL</p>
                    <p class="text-light fst-italic small">
                        It’s not just food,<br />it’s the experience.
                    </p>
                </div>
                <div class="col-5 d-flex justify-content-center">
                    <img class="logo img-fluid" src="/images/The Social Logo.png" style="max-height: 80px;" />
                </div>
            </div>

            <!-- Search + Cart -->
            <div class="w-100 d-flex justify-content-between align-items-center px-3 gap-3 mt-3">
                <div class="flex-grow-1 d-flex px-3 py-2 border border-2 rounded-pill border-light text-light align-items-center gap-2">
                    <i class="bi bi-search"></i>
                    <input id="custom-search-input" type="text"
                           placeholder="Search your favorite food"
                           class="border-0 bg-transparent text-light flex-grow-1" />
                </div>
                <a class="text-decoration-none text-yellow position-relative" href="/Order/Cart">
                    <img src="~/Images/Cart.png" class="img-fluid" style="max-height: 28px;" />
                    @if (orders.Count > 0)
                    {
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            @orders.Count
                        </span>
                    }
                </a>
            </div>
            <p class="text-light mt-3">Select by Category</p>
        </div>
    </nav>

    <!-- Categories (scrollable middle content) -->
    <main class="flex-grow-1 overflow-auto px-3 pb-5"
            style="min-height: 30vh">
        <div class="row row-cols-2 g-3">
            @foreach (var cat in Model)
            {
                if (cat.SubCategories.Select(x => x.Products) != null)
                {
                    <div class="col">
                        <a class="text-decoration-none d-block"
                           asp-controller="Order"
                           asp-action="Menu"
                           asp-route-category="@cat.CategoryId">

                            <div class="bg-white position-relative rounded overflow-hidden shadow-sm"
                                 style="aspect-ratio: 1/1;">
                                <!-- Background image -->
                                <img src="/Images/@(cat.CategoryName.Trim().Replace(" ", "").ToLower() + ".png")"
                                     alt="@cat.CategoryName.Trim()"
                                     class="w-100 h-100 object-fit-cover position-absolute top-0 start-0" />

                                <!-- Dark overlay -->
                                <div class="position-absolute top-0 start-0 w-100 h-100"
                                     style="background: linear-gradient(0deg, #FFFFFF00, #010101); z-index:1;"></div>

                                <!-- Category Name (always visible) -->
                                <div class="position-absolute end-100 start-50 translate-middle text-center shadow w-100 px-2 pt-5 mt-2"
                                     style="z-index:2;">
                                    <h6 class="text-white fw-bold text-wrap">
                                        @cat.CategoryName.ToUpper()
                                    </h6>
                                </div>
                            </div>
                        </a>
                    </div>
                }
            }
        </div>
    </main>

    <!-- Bottom Navigation -->
    <footer class="w-100 py-2 border-top d-flex justify-content-evenly align-items-center text-white"
            style="background: #151515; min-height: 60px;">
        <a class="text-decoration-none d-flex flex-column align-items-center" href="/Home/Index">
            <img src="/Images/Home.png" class="img-fluid" style="max-height: 20px; filter: brightness(0) invert(1);" />
            <small class="text-yellow">Home</small>
        </a>
        <a class="text-decoration-none d-flex flex-column align-items-center" href="/Order/MyOrders/@table">
            <img src="/Images/Orders.png" class="img-fluid" style="max-height: 20px; filter: brightness(1) saturate(0%);" />
            <small class="text-decoration-none text-white">Orders</small>
        </a>
    </footer>
</div>
